# Sprint 2 — Rounds, Questions, Validation, Soft Delete

> **Weeks 5–6**

## Goal

Complete the submission flow: users can add rounds, questions, and topic tags; submissions are validated, persisted, editable for 24h, and soft-deletable.

## Why now

Sprint 1 stopped at "Continue → Rounds". This sprint closes the loop so a report is a real, fully-formed record — the input to every read surface from Sprint 3 onward.

## In scope

- Per-round collapsible cards (UI)
- Round fields: `round_type` (enum), `questions[]`, `experience_prose`, `rating` (positive/mixed/negative)
- Question fields: `question_prose`, `topic_tags[]` (≥1 required, from curated set; "suggest new tag → pending")
- Topic-tag taxonomy table with `status` column; seed ~80 tags across SWE/ML/data/SRE
- Validation rules:
  - At least 0 rounds allowed (some reports are just "got rejected at recruiter screen, no detail")
  - But: if `rounds.length > 0`, each round must have `round_type` + `rating`
  - Each question requires ≥1 active tag (pending tags don't count toward this until promoted)
- Submission finalization: writes `interview_report` + `rounds` + `questions` + `report_tags` join rows in a single transaction
- 24h edit window logic (`reports.locked_at = created_at + 24h`)
- Soft delete: `reports.status = 'deleted'`, PII fields nulled after 90 days via worker job
- Submission confirmation email (Resend) — V1's only notification
- Rate limit: 10 submissions/day/user; 1 submission/company/user without admin override (Redis-backed)
- New-user 24h moderation hold (drops after 3 verified submissions)
- Slur / PII / contact-info regex blocks at submission time

## Out of scope

- Aggregation (Sprint 3)
- Search indexing (Sprint 3)
- Display of submitted reports anywhere (Sprint 4)
- Trust-evidence upload UI (Sprint 6, gated by admin queue)
- Karma earn on submission (Sprint 5)

## Deliverables

| Artifact | Where |
|---|---|
| Per-round collapsible cards w/ keyboard-navigable add/remove | `apps/web/components/submit/` |
| `topic_tags` table + seed (~80 tags) | `packages/db/` |
| Submission transaction (`packages/core/reports/submit.ts`) | repo |
| Soft-delete + 90-day PII purge worker job | `apps/worker/jobs/purge-deleted-pii.ts` |
| Confirmation email template | `apps/web/emails/submission-confirmed.tsx` |
| Rate-limit middleware (Redis sliding window) | `packages/core/anti-abuse/` |
| Regex block list (slur, PII like SSN/phone, contact info) | `packages/core/anti-abuse/regex.ts` |
| `docs/adr/0002-validation-and-soft-delete.md` | repo |

## Exit criteria

- [ ] User can complete a full report (top-level + ≥1 round + ≥1 question + ≥1 tag) and submit successfully
- [ ] Report row, rounds, questions, and tag joins all present after submission; verified with a SQL query
- [ ] Within 24h of submission, user sees "Edit" CTA; after 24h, only "Soft delete" remains
- [ ] Soft delete sets `status = 'deleted'`; data still in DB, not visible to anyone but admin
- [ ] Cron / worker runs 90-day PII purge on a fixture row and clears free-text fields
- [ ] Confirmation email arrives within 60s of submission (test on personal email)
- [ ] 11th submission in 24h is rejected with a friendly message
- [ ] Submitting "call me at 555-1234" is rejected with the regex block

## Risks & mitigations

| Risk | Mitigation |
|---|---|
| Per-round UI gets heavy / janky as users add 5+ rounds | RSC + minimal client component per card; measure with React profiler before optimizing. |
| Regex false positives on legitimate technical content (e.g., "implement a `phone` field") | Start permissive; log blocks to a queue table for review rather than hard-reject; tune in Sprint 6 mod tooling. |
| Single submission transaction grows slow with many rounds/questions | Cap at 20 rounds, 30 questions/round (well over realistic max); document in form copy. |

## Dependencies

- Sprint 1 exit criteria met
- `pg_trgm` index from Sprint 1 reused for tag autocomplete

## Day-by-day skeleton

| Day | Focus |
|---|---|
| 1 | Round/question/tag schema migrations; topic-tag seed |
| 2 | Per-round card UI; add/remove rounds & questions |
| 3 | Tag combobox (reuse Sprint 1 component); "suggest new tag" path |
| 4 | Validation layer (Zod, server-side); error UX |
| 5 | Submission transaction; integration test |
| 6 | 24h edit window + edit UI (same form, prefilled) |
| 7 | Soft delete + PII purge worker job + cron schedule |
| 8 | Rate-limiting middleware + regex block list |
| 9 | Confirmation email; new-user hold logic |
| 10 | E2E happy path + abuse-path tests; exit-criteria walkthrough |

## Notes & decisions

_Append-only._

- 2026-06-02: **Sprint 1 handoff / prep notes** (written at Sprint 1 Day 10). Read before starting.
  - **Reuse, don't rebuild:**
    - `<Combobox>` (`apps/web/components/ui/combobox.tsx`) is the tag input (Day 3). Pass `onSuggestNew` to get the suggest-new-tag row (companies use it; roles don't). Hidden `name` prop posts the selected id in a plain form. Debounced async `search` prop — point it at a new `/api/taxonomy/tags` lookup.
    - The taxonomy lookup pattern is set: `searchCompanies`/`searchRoles` + `pg_trgm` GIN indexes + the `taxonomy_aliases_text()` IMMUTABLE wrapper (migration 0002). Mirror it for `searchTopicTags`. At ~80 tags the planner will still pick the btree/status filter over the trigram index (same as companies at 30 rows) — assert timing + index existence, **not** a forced query plan.
    - **Suggest-pending is wired and is your template.** Sprint 1 Day 10 added `suggestPendingCompany` (server action) → `suggestCompany` (db, idempotent on slug, inserts `status='pending'`/`source='user_suggested'`, attributes `suggestedByUserId`). Build `suggestTag`/`suggestPendingTag` the same way. The "≥1 active tag per question; pending tags don't count until promoted" rule = the active-vs-pending distinction already modeled for companies (`existing` vs `suggested` in `companySelectionSchema`).
    - **Anti-abuse** currently = honeypot in `packages/shared/src/anti-abuse.ts` (pure `isHoneypotTripped`, silent-drop pattern) + account-boundary Clerk captcha. This sprint adds Redis rate-limit + regex blocks in `packages/core/anti-abuse/`. The honeypot's **fail-closed-without-tipping-off** shape is the model for how the rate-limit/regex paths should reject. Keep the honeypot where it is (pure, dep-free) unless `core` needs it too.
    - **E2E harness is ready to extend** (`apps/web/e2e/`, Day 9): Playwright + `@clerk/testing`, system Chrome (`channel:"chrome"`, no browser download), `clerkSetup()` testing-token bypass of the sign-up Turnstile, email-ticket sign-in, idempotent `+clerk_test` user, `CLERK_TELEMETRY_DISABLED=1` so the worker exits. Add the Sprint 2 Day 10 happy-path + abuse-path specs here. Note the gotcha: a native `<select>` resolves via `page.locator("select")`, not `getByLabel` (Chromium folds the aria-hidden `*` into the implicit-label name).
    - **Draft persistence** (`submission_drafts.data` jsonb + `submissionDraftSchema`, everything `.nullish()`) is the autosave substrate — extend the schema with `rounds[]`/`questions[]`/`tags[]`; the 2s debounce + resume route already work.
  - **Schema cutovers you own this sprint:**
    - `interview_reports.level_id` (FK → `company_levels`, nullable) was added Day 1 **alongside** the legacy text `level` (still NOT NULL); no reports were written in Sprint 1. The wedge index `reports_company_role_level_idx` is on **text `level`** and is asserted by `query-plan.test.ts`. When the finalization transaction first writes reports, decide the text→FK cutover (populate both, or migrate the index + its test). This is the deferred decision from Day 1.
    - **Draft → report finalization** must resolve the draft's company to a real id: a draft may still hold a `kind:"suggested"` company (name only) if the user never hit Continue, or a backfilled `existing` id if they did. Re-run `suggestCompany` (idempotent) during finalization to be safe — Sprint 1 deliberately does **not** force-persist the backfilled id into the draft before navigating. Same resolve-on-finalize pattern applies to suggested tags.
  - **ADR numbering:** this file's deliverables list says `docs/adr/0002-validation-and-soft-delete.md`, but **0002 is taken** (ORM choice). The ADR roadmap (`docs/adr/README.md`) reserves **0004** for "Validation rules and soft-delete semantics" — write **ADR-0004**. (Day 7's i18n ADR took 0003.)
  - **i18n:** all copy lives in `messages/en.json` (next-intl, single-locale no-prefix — ADR-0003). The `rounds` namespace already exists (Sprint 1's stub) — expand it; add a `questions`/`tags` namespace. Server components use `getTranslations`, client `useTranslations`; rich text via `t.rich`, dynamic labels via template keys (`t(\`outcome.${o}\`)`).

- 2026-06-02: **Days 1–2 done** (schema/seed + per-round UI). 79 db tests + 4 shared tests green; web typecheck + prod build clean.
  - **Day 1 — topics taxonomy.** Extended the `topics` table to mirror companies/roles: added `aliases`, `status` (default `active`), `source` (default `user_suggested`), `suggested_by_user_id` (FK→users, SET NULL), + `topics_status_idx`. Migration **0003** (drizzle-generated columns) + **0004** (hand-written trgm indexes `topics_name_trgm_idx` / `topics_aliases_trgm_idx`, mirroring 0002 — pg_trgm + `taxonomy_aliases_text()` already exist from 0002). 0004 was wired into the journal + a meta snapshot copied from 0003 (new `id`, `prevId`→0003), same pattern 0002 used; `drizzle-kit generate` now reports no pending diff. Seeded **88** curated topics (`CURATED_TOPICS`) across algorithms/system-design/fundamentals/ML/data/SRE + behavioral (test asserts the ≥80 floor, not an exact count). Added `searchTopics` + `suggestTopic` to `packages/db/src/taxonomy.ts` — topics are the *second* suggest-new taxonomy (roles still closed). **`/api/taxonomy/topics` route + the tag combobox are Day 3** (not built yet).
  - **Day 2 — per-round card UI.** Extended `submissionDraftSchema` with `rounds[]` (each round: `roundType`/`rating`/`experience` + `questions[]`; each question: `prose` + `tags[]` via `topicTagSelectionSchema`, the existing-vs-suggested union mirroring company). All `.nullish()` so pre-Sprint-2 drafts still parse. Added caps `MAX_ROUNDS=20` / `MAX_QUESTIONS_PER_ROUND=30` + `topicSuggestionSchema` (for the Day 3 action). New `RoundsForm` client component (`apps/web/app/submit/rounds/rounds-form.tsx` + `.module.css`): collapsible cards, keyboard-navigable add/remove rounds & questions (focus moves to new card / back to add-button on remove), per-round type select + rating chips + experience textarea, per-question prose textarea. **Tag input is a placeholder hint** (`tags.comingSoon`) — Day 3 drops in the `<Combobox>`. Question `tags[]` currently serialized as `[]`.
  - **Draft continuity decision.** The basics form's Continue now **persists the draft synchronously** and forwards its id as `/submit/rounds?draft=<id>` (the 2s autosave can't be relied on for a fast Continue). The rounds page is ownership-scoped via `getDraft` (no draft id → redirect `/submit`; foreign id → 404). To avoid clobbering, the basics form's draft payload **passes through `initialData.rounds`** (it doesn't edit them), and `RoundsForm` saves `{...initialData, rounds}` so basics survive a rounds-only save. The "Back to basics" link points at `/drafts/<id>` (full RSC reload re-hydrates latest rounds).
  - **Still deferred to Day 5 (unchanged):** the `interview_reports` text-`level` → `level_id` FK cutover + the `reports_company_role_level_idx` / `query-plan.test.ts` migration. No reports are written yet, so untouched. Resolve-on-finalize (idempotent `suggestCompany`/`suggestTopic`) for any `kind:"suggested"` company/tag still applies at the transaction (Day 5).

- 2026-06-03: **Days 3–4 done** (tag combobox + suggest-new path; finalize validation layer + inline error UX). All tests green (shared 28, db 79), web typecheck + lint + prod build clean. Verified Days 1–2 still green first.
  - **Day 3 — tag combobox.** New `/api/taxonomy/topics` route mirrors `/companies` (suggest-new ON; `canSuggestNew` when a non-empty query has no active match). Client `searchTopics` in `rounds-form/api.ts` returns the full `{id,slug,name}` match (the picker needs the slug to build an `existing` selection — the combobox option only carries id+label). New `suggestPendingTopic` server action mirrors `suggestPendingCompany` exactly (rate-limited via new `RATE_LIMITS.suggestTopic` = 20/hr, honeypot → `actionOk(null)`, idempotent `suggestTopic`, returns id/slug/name). **Multi-select:** added a `clearOnSelect` prop to `<Combobox>` — on pick/suggest it clears the input and stays open/focused instead of mirroring the label, so several tags can be added without remounting (no focus loss). New `rounds-form/tag-picker.tsx` wraps that combobox + removable `FtlTag` chips; the parent owns `TopicTagSelection[]`, combobox stays `value=null`. Dedupe by id (existing) / case-folded name (suggested); suggested chips render a muted "pending" affordance. Wired tags through the rounds state: `Question.tags`, `newQuestion`, `from/toDraftRounds` (was serializing `[]`), `patchQuestionTags` in the form, `onPatchQuestionTags` into `RoundCard`. Replaced the Day-2 `tags.comingSoon` placeholder with the live picker; `tags` i18n namespace reworked (label/placeholder/empty/suggestNew/selectedLabel/pending/remove).
  - **Day 4 — validation layer.** `validateFinalSubmission(data: unknown): SubmissionValidation` in `packages/shared/src/submission.ts` is the single server-side finalize gate (Day 5's transaction will call it before writing). Re-parses the payload with the tolerant `submissionDraftSchema`, then applies the strict rules **in code** (clearer than Zod refinements for cross-field/per-index rules): 0 rounds OK; a round needs `roundType`+`rating`; a question needs non-blank prose + **≥1 *active* (existing)** tag — a pending/suggested tag does **not** count (matches the searchTopics active-only filter). Returns either the narrowed `FinalSubmission` (trims prose, blank experience → null) or a structured `SubmissionIssues` (per-field booleans + per-round/per-question arrays, index-aligned with the form) for inline rendering; a non-parseable payload → `{malformed:true}`. 12 new shared tests cover every rule incl. the suggested-tag-doesn't-count case. **Error UX:** the rounds form computes `validateFinalSubmission({...initialData, rounds})` live (memoized) and threads each round's issues into its `RoundCard`; cards show inline errors under the type select / rating chips / question prose / tags — but **held back for pristine cards** (`roundIsPristine`/`questionIsPristine`) so a freshly-added round/question isn't pre-emptively flagged. Basics-field issues are computed but not rendered here (they belong to the prior screen).
  - **Deferred to Day 5 (unchanged):** the text-`level`→`level_id` cutover + query-plan test migration; the actual Submit button + finalize transaction (which will call `validateFinalSubmission` server-side, then resolve-on-finalize the suggested company/tags via idempotent `suggestCompany`/`suggestTopic` and write `interview_report`+`rounds`+`questions`+`report_tags` in one transaction).

- 2026-06-03: **Days 5–6 done** (submission transaction + integration test; 24h edit window + edit UI). All tests green (shared 28, db 86 incl. new `reports.test.ts`, core 6 — a new package test suite); web typecheck + lint + prod build clean (`/reports/[id]` route present).
  - **Schema cutovers resolved.** (1) **Level → populate both.** The finalize write sets `interview_reports.level` (text = the level name, drives the wedge index) **and** `level_id` (the FK, nullable; null = the "N/A" sentinel). The wedge index stays on text `level`; `query-plan.test.ts` is untouched — the actual index→FK migration is still deferred to a later sprint, as the Day-1 note allowed. (2) **`report_tags` is `question_topics`.** The deliverables table's "report_tags" was a misnomer — tags attach to **questions** (`question_topics`), not reports. The finalize write inserts `question_topics` join rows. (3) **New `interview_month` column.** Sprint 1 collected a *required* month but `interview_reports` had nowhere to store it (it only lived in the draft jsonb); finalize is the first writer, so Day 5 added `interview_month text NOT NULL` (stores the "YYYY-MM" the form gives; Sprint 3 aggregation groups on it). Existing `constraints.test.ts` report inserts were updated with the new field.
  - **Day 5 — finalize transaction.** New `interview_reports.locked_at` (migration **0005**, alongside `interview_month`): a plain column **defaulted to `now() + interval '24 hours'`**, *not* a generated column — `timestamptz + interval` isn't IMMUTABLE so PG rejects it as a generation expression, but a column default has no such constraint, and both `now()` calls in one insert resolve to the same txn timestamp so `locked_at == created_at + 24h` exactly. New `packages/db/src/reports.ts` is the pure persistence layer (no shared/core dep): `createReport` (one tx: report → ordered rounds → ordered questions → deduped `question_topics`), `updateReport` (edit-in-place: delete the report's rounds — children CASCADE — update top-level columns, re-insert children, **leaving `created_at`/`locked_at` untouched** so an edit never slides the window), `getReport`/`getReportForEdit` (ownership-scoped; the latter folds the joined rounds→questions→topics tree), `isReportEditable`, `EDIT_WINDOW_MS`, + `deleteDraft` in `drafts.ts`. The orchestrator `finalizeSubmission` lives in **core** (`packages/core/src/reports/submit.ts`): re-validates via shared's `validateFinalSubmission`, resolves suggested company/tags to pending rows via idempotent `suggestCompany`/`suggestTopic` **outside the tx** (harmless if the write later fails), gates an edit on ownership + window *before* any resolution write, then create-or-update + consumes the draft. **Core got its first test suite** (`vitest.config.ts` + `tests/global-setup.ts` mirroring db's testcontainer, migrations resolved from `../../db/src/migrations`). Web: `finalizeSubmissionAction` (auth + new `RATE_LIMITS.submitReport` 10/day fixed-window + honeypot fail-closed → `actionOk(null)` + ownership draft load), a primary **Submit** button on the rounds form (enabled only when `validateFinalSubmission` is `ok`; flushes a synchronous save *then* finalizes so it never finalizes a stale draft; routes to `/reports/[id]`), `routes.report`, `rounds.submit` i18n.
  - **Day 6 — edit window + edit UI.** `submissionDraftSchema` gained `editingReportId` (nullish). New owner-only `/reports/[id]` page (RSC, `getReportForEdit` → 404 if not owner) doubles as the post-submit landing: summary + status + **Edit** CTA with hours-left while `isReportEditable`, or a "locked" notice after 24h. The **Edit** path is a server action (`reports/[id]/actions.ts`) that re-checks ownership + window, rehydrates the report into a temp draft via core's `reportDetailToDraft` (everything as `kind:"existing"`, `editingReportId` stamped on), and redirects to `/submit/rounds?draft=<temp>` — so "edit" reuses the whole form and finalize updates in place. `SubmitForm` now threads `editingReportId` through its autosave payload so editing basics (via the existing `/drafts/[id]` resume) can't strip it and silently fork a new report. `report` i18n namespace added.
  - **Still deferred (Day 7+, unchanged):** soft delete + 90-day PII purge worker (Day 7), Redis sliding-window + 1/company/user limits + regex block list (Day 8), confirmation email + new-user moderation hold (Day 9), E2E specs (Day 10), and **ADR-0004** (validation + soft-delete semantics — better written once Day 7's soft-delete lands). The text-`level`→`level_id` index cutover + its `query-plan.test.ts` migration also remain deferred (we populate both columns for now).
  - **Heads-up (not mine):** `apps/web/components/ui/site-header.tsx` was modified mid-session by something outside this work (dropped the "· for engineers, by engineers" tagline, left trailing whitespace). Left untouched and **excluded from these commits** — flag for whoever owns that edit.

- 2026-06-03: **Days 7–8 done** (soft delete + 90-day PII purge worker/cron; sliding-window rate limit + per-company cap + regex block list). All tests green (shared 28, db 90 incl. 4 new soft-delete/purge cases, core 20 incl. new regex unit suite + blocked/duplicate-company integration cases); web typecheck + lint + prod build clean; worker typecheck + build clean. **ADR-0004 written** (validation + soft-delete + anti-abuse) and the index row flipped to accepted.
  - **Day 7 — soft delete + PII purge.** Migration **0006** adds two nullable timestamptz columns to `interview_reports`: `deleted_at` (stamped on soft delete) and `pii_purged_at` (stamped once the purge scrubs the row). db layer (`reports.ts`): `softDeleteReport` (ownership-scoped, `ne(status,'deleted')` so a double-delete can't re-stamp `deleted_at` and slide the purge clock; flips status→`deleted` + sets `deleted_at`), `PII_RETENTION_MS` (90d), and `purgeDeletedReportPii(db, before)` — one tx that finds `status='deleted' AND deleted_at < before AND pii_purged_at IS NULL`, nulls round `experience_prose`, redacts question `question_prose` to `''` (NOT NULL column, so empty string is the redaction; resolved via round ids since questions carry `round_id` not `report_id`), stamps `pii_purged_at`. Idempotent. Web: `softDeleteReportAction` in `reports/[id]/actions.ts` (re-renders the owner view into its deleted state via `revalidatePath` + redirect), a small `DeleteReportButton` client component (confirm() guard only — the action re-checks auth/ownership). `/reports/[id]` now has three action states: **editable** (Edit + Delete), **locked** (Delete only — "after 24h, only soft delete remains"), **deleted** (notice, no actions). `report.delete` i18n added. **Worker:** `apps/worker/src/jobs/purge-deleted-pii.ts` + a daily BullMQ **JobScheduler** (`upsertJobScheduler`, cron `17 3 * * *`, idempotent on scheduler id) wired in `index.ts` alongside the hello worker; shutdown now closes all workers + the purge queue + `closeDb()`. The worker gained its **first DB dependency**, so it now loads env from the repo root (`src/env.ts`, dotenv, imported before Sentry) — previously it only used `REDIS_URL` (which had a localhost default) and never loaded `.env`. Added `dotenv` to the worker (pinned `^17.4.2` to match db).
  - **Day 8 — anti-abuse.** (1) **Regex block list** lives in **core** (`packages/core/src/anti-abuse/regex.ts`, the deliverable's stated home — not shared, because the finalize gate that enforces it is in core), two severities: contact-info/PII (email, 10-digit + bare 7-digit phone like `555-1234`, US SSN) = **block**; profanity word-list = **flag** (returned, never rejected — sprint risk note: candor is the product, "the interviewer was an ass" must pass). Patterns match contact-info *shapes*, never the words "phone"/"email", so technical prose stays clean (unit-tested). Identity slurs belong at block severity but the curated list is maintained operationally, kept out of source. `firstBlockingMatch` is the gate. (2) **finalizeSubmission** (core) gained two gates: regex block on the trimmed free text (before any write → new result `{reason:"blocked", category}`), and a **per-company cap** via `userHasReportForCompany` (db: any non-deleted report for this user+company → `{reason:"duplicate_company"}`; create-path only, after company resolution so a suggested company compares by real id; soft-deleting frees the slot). (3) **Sliding-window rate limit** (`apps/web/lib/rate-limit.ts` `slidingWindowRateLimit`, Redis sorted-set, fail-open) now backs the 10/day `submitReport` cap (no midnight-boundary doubling); the generous autosave/suggest budgets stay on the fixed-window counter. `finalizeSubmissionAction` switched to it and maps the two new reasons to friendly messages.
  - **Deviations from the sprint deliverables (documented in ADR-0004):** the per-company "1/company/user" is a **durable DB check, not "Redis-backed"** — it's a standing invariant, not a time box, and must survive a Redis flush. The sliding-window rate limiter stays in `apps/web/lib` (web infra), **not** moved to `packages/core/anti-abuse/` — core is domain logic shared with the worker and shouldn't take an ioredis dep; only the pure regex block list went to core/anti-abuse as specified.
  - **Still deferred (Day 9+):** confirmation email (Resend) + new-user 24h moderation hold (Day 9), E2E happy-path + abuse-path specs (Day 10). The text-`level`→`level_id` index cutover + its `query-plan.test.ts` migration also still deferred (both columns populated).
  - **⚠️ Worker redeploy needed (manual).** The worker image + compose are hand-shipped to Hetzner and drift silently from the repo. This sprint changed worker code materially (new `purge-deleted-pii` queue + cron, **new `DATABASE_URL` requirement**, dotenv env-loading). The deployed worker must be **rebuilt and re-shipped**, and its compose env must include `DATABASE_URL`, or the purge cron will throw when it first fires.

- 2026-06-03: **Days 9–10 done** (confirmation email + new-user hold; E2E happy/abuse specs + exit-criteria walkthrough) **+ the prod-worker `DATABASE_URL` wiring** the Day 7–8 heads-up flagged. All tests green (shared 28, db 91 incl. verified-count test, core 24 incl. new moderation unit suite); web typecheck + lint + prod build clean; worker typecheck + build clean; the 5 Playwright specs compile/list.
  - **Prod-worker DBURL fix.** `infra/hetzner/docker-compose.prod.yml`'s worker service now passes `DATABASE_URL=${DATABASE_URL:?…}` (required — fail fast at boot, not at first cron fire) plus `RESEND_API_KEY`/`RESEND_FROM_EMAIL`; `.env.prod.example` documents all three. The image still ships manually (memory note unchanged), but the compose source is now correct.
  - **Day 9 — confirmation email.** Architecture: **web renders, worker dispatches** (matches the worker README's "notifications queue → Resend"). New shared contract `packages/shared/src/notifications.ts` (`NOTIFICATIONS_QUEUE`, `EMAIL_JOB`, `EmailJobData{to,subject,html,text}`) so neither side imports the other. `apps/web/emails/submission-confirmed.ts` renders the email **as an inline-styled HTML string** — *not* a rendered React component: Next forbids `react-dom/server` in the server-action graph (the build failed on the first `.tsx` attempt), and email clients want inline styles anyway, so the deliverable's `.tsx` became `.ts`. `finalizeSubmissionAction` enqueues it best-effort after a successful **create** (never on edit; own try/catch so an enqueue failure can't fail the submission; no-ops without a recipient) via new `getNotificationsQueue` in `lib/queue.ts`, with `attempts:3` + exponential backoff. Worker: `apps/worker/src/jobs/send-email.ts` lazily builds a Resend client (blank `RESEND_API_KEY` → logged no-op, not a crash, matching the Sentry/Typesense convention) and is wired as a third Worker in `index.ts`. Added `resend` to worker deps.
  - **Day 9 — new-user hold.** Pure policy `decideInitialReportStatus({accountAgeMs, verifiedReportCount})` in `packages/core/src/reports/moderation.ts`: a report publishes **`active`** only when the account is past the 24h floor **AND** has ≥3 verified submissions (`NEW_USER_HOLD_MS`, `TRUSTED_VERIFIED_THRESHOLD`); otherwise **`pending_moderation`** (the column default). Conservative AND-reading of "24h hold (drops after 3 verified submissions)" — documented in-module. db: `countVerifiedReportsForUser` (non-deleted reports with `evidence_verified=true`) + `getUserById` (for `created_at`). `ReportWriteInput.status` is now optional and `createReport` honors it (omit → default); `finalizeSubmission`'s create path computes it. **In V1 nothing sets `evidence_verified` yet, so every report still lands `pending_moderation` — behavior is unchanged; the policy is just encoded for Sprint 6 verification/mod tooling.** Edits never re-decide status.
  - **Day 10 — E2E + walkthrough.** New `apps/web/e2e/helpers.ts` (shared `signIn`/`fillCombobox`/`fillBasics`/`addRoundWithQuestion`), `report-lifecycle.spec.ts` (submit a full report → land on `/reports/[id]` → Edit → soft-delete; deletes at the end so the per-company cap doesn't block reruns), and `abuse.spec.ts` (the "call me at 555-1234" regex block at submit). e2e README gained a **Sprint 2 exit-criteria walkthrough** table mapping each criterion to its E2E or integration/unit coverage. **Note:** the new specs are authored against current component source and compile/list under Playwright, but were **not executed in this session** (they need the gated env — `CLERK_SECRET_KEY` + seeded DB + system Chrome). Duplicate-company and rate-limit abuse paths are covered at the core/db layer rather than E2E (driving 2+ full browser submissions is brittle).
  - **Sprint 2 status:** all 10 days complete. Deferred onward (not Sprint 2 scope): the text-`level`→`level_id` index cutover + its `query-plan.test.ts` migration; admin/moderation tooling + the `evidence_verified` write path + the profanity-flag review queue (Sprint 6); aggregation/read surfaces (Sprint 3–4).

- 2026-06-04: **Worker prod-deploy hotfix — bundle workspace deps.** First real prod deploy of the Sprint 2 worker (purge-cron + email) crash-looped on the box with `ERR_UNKNOWN_FILE_EXTENSION: ".ts"` for `@fromtheloop/db/src/index.ts`. Root cause: the worker ran `node dist/index.js` (plain `tsc` build, no bundling), but the `@fromtheloop/*` workspace packages declare their entry as raw `./src/index.ts` (fine for Next's `transpilePackages` + tsx/vitest; **not** loadable by plain `node`). The DB dep was added Day 7, so this path never executed on the box until now — classic silent drift (the worker image is hand-shipped). **Fix (worker-only):** new `apps/worker/build.mjs` esbuild bundle that inlines the `@fromtheloop/*` source into `dist/index.js` and keeps every real npm dep external (bullmq's Lua scripts, pg, etc. must not be inlined); `package.json` `build` script `tsc -p` → `node build.mjs` (typecheck still `tsc --noEmit`). Because bundling lifts db/core/shared's *transitive* third-party deps to the top level, the worker must now declare them directly — added `drizzle-orm`/`postgres`/`zod` (matching db/shared's exact ranges; single lockfile dedupes, no drift) so `pnpm deploy --prod` symlinks them at the top level (they were previously only resolvable from db's nested `.pnpm` dir). Verified by replicating the Dockerfile's `deploy --prod` layout and booting the bundle (loads all modules, reaches runtime — only fails on a deliberately-bogus Redis port). No Dockerfile change needed (it already `COPY`s the worker dir + runs `pnpm build`). **Do not revert the build back to `tsc`** — it will reintroduce the crash. Redeployed to the box; worker confirmed `Up` (green).
