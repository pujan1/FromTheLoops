{
  "name": "@fromtheloop/core",
  "version": "0.0.0",
  "private": true,
  "description": "Domain logic: reports, karma, trust scoring, moderation, taxonomy.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "lint": "echo 'TODO: eslint .'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fromtheloop/db": "workspace:*",
    "@fromtheloop/shared": "workspace:*"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^12.0.0",
    "@types/node": "^20.14.0",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9",
    "testcontainers": "^12.0.0",
    "vitest": "^4.1.7"
  }
}
