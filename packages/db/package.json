{
  "name": "@fromtheloop/db",
  "version": "0.0.0",
  "private": true,
  "description": "Postgres schema, Drizzle ORM, migrations, query helpers.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "build": "echo 'TODO: tsc -p tsconfig.json'",
    "lint": "echo 'TODO: eslint .'",
    "typecheck": "echo 'TODO: tsc --noEmit'",
    "test": "echo 'no tests yet'",
    "generate": "echo 'TODO: drizzle-kit generate'",
    "migrate": "echo 'TODO: drizzle-kit migrate'",
    "studio": "echo 'TODO: drizzle-kit studio'",
    "seed": "echo 'TODO: tsx src/seed/index.ts'"
  }
}
