import { PlaceholderPage } from "../_components/placeholder-page";

export default function CompaniesPage() {
  return (
    <PlaceholderPage
      eyebrow="companies"
      title="Company pages are being assembled."
      body="This route is connected so company navigation has a real landing surface while the experience index and filters come online."
      tags={["company profiles", "interview loops", "verified experiences"]}
    />
  );
}
