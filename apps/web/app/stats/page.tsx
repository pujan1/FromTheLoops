import { PlaceholderPage } from "../_components/placeholder-page";

export default function StatsPage() {
  return (
    <PlaceholderPage
      eyebrow="stats"
      title="Corpus numbers are connected."
      body="This route is ready for aggregate report counts, verified contributor ratios, and interview-round coverage."
      tags={["reports", "rounds", "verified", "topics"]}
    />
  );
}
